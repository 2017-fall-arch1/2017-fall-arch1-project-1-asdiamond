/* this function is pretty useful for reading
input from a stream line by line, might as
well use it... */
char buf[100];
int gets_n(char *s, int limit, FILE *stream);

void addEmployees(BinarySearchTree *bst);
